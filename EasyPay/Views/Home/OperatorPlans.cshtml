@*@model EasyPay.Models.ServiceOperatorPlan*@
@model IEnumerable<EasyPay.Models.ServiceOperatorPlan>

<style>
    .btn-primary {
        color: #fff;
        background-color: #337ab7;
        border-color: #2e6da4;
    }

    .btn {
        display: inline-block;
        padding: 6px 12px;
        margin-bottom: 0;
        font-size: 14px;
        font-weight: 400;
        line-height: 1.42857143;
        text-align: center;
        white-space: nowrap;
        vertical-align: middle;
        -ms-touch-action: manipulation;
        touch-action: manipulation;
        cursor: pointer;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
        background-image: none;
        border: 1px solid transparent;
        border-radius: 4px;
    }
    .tabs-menu {
            height: 30px;
            float: left;
            clear: both;
        }

            .tabs-menu li {
                height: 30px;
                line-height: 30px;
                float: left;
                margin-right: 10px;
                background-color: #ccc;
                border-top: 1px solid #d4d4d1;
                border-right: 1px solid #d4d4d1;
                border-left: 1px solid #d4d4d1;
            }

                .tabs-menu li.current {
                    position: relative;
                    background-color: #fff;
                    border-bottom: 1px solid #fff;
                    z-index: 5;
                }

                .tabs-menu li a {
                    padding: 10px;
                    text-transform: uppercase;
                    color: #fff;
                    text-decoration: none;
                }

            .tabs-menu .current a {
                color: #2e7da3;
            }

        .tab {
            border: 1px solid #d4d4d1;
            background-color: #fff;
            float: left;
            margin-bottom: 20px;
            width: auto;
        }

        .tab-content {
            width: 660px;
            padding: 20px;
            display: none;
        }

        #tab-1 {
            display: block;
        }

        table {
            border-collapse: collapse;
        }

        table, th, td {
            border: 1px solid black;
        }

        th {
            font-weight: bold;
        }

        th, td {
            padding: 10px;
            text-align: left;
        }
</style>
<script>
    $(document).ready(function () {
        $(".home_tabs a").click(function (event) {
            event.preventDefault();
            $(this).parent().addClass("current");
            $(this).parent().siblings().removeClass("current");
            var tab = $(this).attr("href");
            $(".tab-content").not(tab).css("display", "none");
            $(tab).fadeIn();
        });
    });

    function fillAmount(id) {
        $("#amtFill").val(id);
        //window.location.assign("Index", id)
    }
</script>

@if (Model == null)
{
    <div></div>
    @*<div id="ad_img">

            <img src="images/ad1.jpg" width="408" height="224" />

            <div class="ad_text">Stay tuned for best offers !</div>

        </div>*@
}

else
{

    <div id="tabs-container">
        @*<ul class="tabs-menu" style="list-style-type:none">
                <li class="current"><a href="#tab-1">Top Up</a></li>
                <li><a href="#tab-2">Special Recharge</a></li>
                <li><a href="#tab-3">Full Talk Time</a></li>
                <li><a href="#tab-4">3G</a></li>
                <li><a href="#tab-5">2G</a></li>
            </ul>*@
        <ul class="home_tabs">
            <li><a href="#tab-1"><span>Top Up</span></a></li>
           
            <li><a href="#tab-2"><span>Special Recharge</span></a></li>
            <li><a href="#tab-3"><span>Full Talk Time</span></a></li>
            <li><a href="#tab-4"><span>3G</span></a></li>
            <li><a href="#tab-5"><span>2G</span></a></li>
        </ul>

        <div class="tab">
            
                <div id="tab-1" class="tab-content">
                    <table>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Validity)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Description)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Price)
                            </th>
                        </tr>
                        @foreach (var item in Model)
                    {
                        if (item.PlanName == "Top Up")
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Validity)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    <input type="button" class="btn btn-primary" value="@item.Price" onclick="fillAmount(@item.Price)" />
                                    @*@Html.DisplayFor(modelItem => item.Price)*@
                                </td>
                            </tr>
                        }
                    }
                    </table>
                </div>
                <div id="tab-2" class="tab-content">
                    <table>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Validity)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Description)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Price)
                            </th>
                        </tr>
                        @foreach (var item in Model)
                    {
                        if (item.PlanName == "Special Recharge")
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Validity)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    <input type="button" class="btn btn-primary" value="@item.Price" onclick="fillAmount(@item.Price)" />
                                    @*@Html.DisplayFor(modelItem => item.Price)*@
                                </td>
                            </tr>
                        }

                    }
                    </table>
                </div>
                <div id="tab-3" class="tab-content">
                    <table>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Validity)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Description)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Price)
                            </th>
                        </tr>
                        @foreach (var item in Model)
                    {
                        if (item.PlanName == "Full Talk Time")
                        {
                            <tr>
                                <td>

                                    @Html.DisplayFor(modelItem => item.Validity)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    <input type="button" class="btn btn-primary" value="@item.Price" onclick="fillAmount(@item.Price)" />
                                    @*@Html.DisplayFor(modelItem => item.Price)*@
                                </td>
                            </tr>
                        }
                    }
                    </table>
                </div>
                <div id="tab-4" class="tab-content">
                    <table>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Validity)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Description)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Price)
                            </th>
                        </tr>
                        @foreach (var item in Model)
                    {
                        if (item.PlanName == "3G")
                        {
                            <tr>
                                <td>

                                    @Html.DisplayFor(modelItem => item.Validity)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    <input type="button" class="btn btn-primary" value="@item.Price" onclick="fillAmount(@item.Price)" />
                                    @*@Html.DisplayFor(modelItem => item.Price)*@
                                </td>
                            </tr>
                        }

                    }
                    </table>
                </div>
                <div id="tab-5" class="tab-content">
                    <table>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Validity)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Description)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Price)
                            </th>
                        </tr>
                        @foreach (var item in Model)
                    {
                        if (item.PlanName == "2G")
                        {
                            <tr>
                                <td>

                                    @Html.DisplayFor(modelItem => item.Validity)

                                </td>
                                <td>

                                    @Html.DisplayFor(modelItem => item.Description)

                                </td>
                                <td>

                                    <input type="button" class="btn btn-primary" value="@item.Price" onclick="fillAmount(@item.Price)" />
                                    @*@Html.DisplayFor(modelItem => item.Price)*@

                                </td>
                            </tr>
                        }
                    }
                    </table>
                </div>
            </div>
    </div>
    @*<style>
            table {
                border-collapse: collapse;
            }

            table, th, td {
                border: 1px solid black;
            }

            th {
                font-weight: bold;
            }

            th, td {
                padding: 10px;
                text-align: left;
            }
        </style>
        <br /><br /><br /><br />
        <div id="ad_img">

            <table>
                <tr>

                    <th>

                        @Html.DisplayNameFor(model => model.PlanName)

                    </th>
                    <th>

                        @Html.DisplayNameFor(model => model.Validity)

                    </th>
                    <th>

                        @Html.DisplayNameFor(model => model.Description)

                    </th>
                    <th>

                        @Html.DisplayNameFor(model => model.Price)

                    </th>


                </tr>

                @foreach (var item in Model)
                {

                    <tr>

                        <td>

                            @Html.DisplayFor(modelItem => item.PlanName)

                        </td>
                        <td>

                            @Html.DisplayFor(modelItem => item.Validity)

                        </td>
                        <td>

                            @Html.DisplayFor(modelItem => item.Description)

                        </td>
                        <td>

                            @Html.DisplayFor(modelItem => item.Price)

                        </td>

                        <td>
                            <a title="Select" ng- href="">
                                <img src="~/Images/forms-icon.png" alt="Select" />
                            </a>
                        </td>
                    </tr>
                }
            </table>
        </div>*@
}